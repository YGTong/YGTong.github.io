const toast=document.querySelector('#toast');function notify(message){toast.textContent=message;toast.classList.add('show');window.setTimeout(()=>toast.classList.remove('show'),1800)}document.querySelectorAll('.filters button').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('.filters button').forEach(item=>item.classList.remove('active'));button.classList.add('active');const filter=button.dataset.filter;document.querySelectorAll('.publication').forEach(publication=>publication.classList.toggle('hidden',filter!=='all'&&publication.dataset.status!==filter))}));document.querySelectorAll('.cite-button').forEach(button=>button.addEventListener('click',async()=>{await navigator.clipboard.writeText(button.dataset.cite);notify('BibTeX copied')}));document.querySelector('#copy-email').addEventListener('click',async event=>{await navigator.clipboard.writeText(event.currentTarget.dataset.email);notify('Email address copied')});document.querySelector('#year').textContent=new Date().getFullYear();
